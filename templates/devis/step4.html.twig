{% extends 'base_dashboard.html.twig' %}

{% block title %}Ton devis — Mode Manuel{% endblock %}

{% block body %}
<main class="container py-4">
    <div class="card-soft p-4 p-md-5">
            <!-- Header -->
            <div class="d-flex align-items-center justify-content-between mb-3">
                <h2 class="mb-0">Aperçu du devis</h2>
                <div class="text-end small text-secondary">
                    <div>Devis <strong>#{{ devis.number }}</strong> — <span class="text-muted">{% if devis.archivedAt is not null %}Archivé{% elseif devis.sendedAt is not null %}Envoyé{% else %}Brouillon{% endif %}</span></div>
                </div>
            </div>
            <hr class="divider">

            <!-- Header info + actions -->
            <div class="row g-4 align-items-start mb-4">
                <div class="col-md-8">
                    <div class="row g-4">
                        <div class="col-sm-6">
                            <div class="d-flex align-items-start gap-2">
                                <span class="badge-circle me-2">S</span>
                                <div>
                                    <div class="fw-bold">{{ devis.nameCompany }}</div>
                                    <div class="text-secondary small">
                                        {{ devis.address }}<br>
                                        {{ devis.postalCodeCompany }} {{ devis.countryCompany }}<br>
                                        {{ devis.phoneNumberCompany }}<br>
                                        {{ devis.emailCompany }}
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-sm-6">
                            <div class="fw-bold">{{ devis.nameCustomer }}</div>
                            <div class="text-secondary small">
                                {{ devis.addressCustomer }}<br>
                                {{ devis.postalCodeCustomer }} {{ devis.cityCustomer }}<br>
                                {{ devis.customer.email }}
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 text-md-end d-flex d-md-block gap-2 no-print">
                    <a class="btn btn-brand text-white w-100 mb-2" href="{{ path("app_devis_view_send", {"id": devis.id}) }}"><i class="bi bi-envelope-fill me-1"></i>Envoyer</a>
                    <a class="btn btn-brand text-white w-100 mb-2" href="{{ path("app_devis_transform_invoice", {"id": devis.id}) }}"><i class="bi bi-file-earmark-check-fill me-1"></i>Transformer en facture</a>
                    <a class="btn btn-brand text-white w-100 mb-2" href="{{ path("app_devis_duplication", {"id": devis.id}) }}"><i class="bi bi-files me-1"></i>Dupliquer</a>
                    <a class="btn btn-ghost w-100 mb-2" href="{{ path("app_devis_duplication", {"id": devis.id}) }}"><i class="bi bi-archive me-1"></i>Archiver</a>
                    <a class="btn btn-ghost w-100" href="{{ path("app_devis_change_style", {"id": devis.id}) }}"><i class="bi bi-brush me-1"></i>Changer de style</a>
                </div>
            </div>

            <!-- Intro -->
            <p class="text-secondary">
                Nous vous proposons une prestation complète, avec une attention particulière aux détails pour un résultat impeccable.
            </p>

            <!-- Section: prestations principales -->
            <h4 class="mt-4">{{ devis.name }}</h4>
            <div class="mt-2">
                {% for article in devis.articles %}
                <div class="li-row">
                    <div>• {{ article.name }} — {{ article.description }}</div>
                    {% if article.price is not null %}
                        <div class="price">{{ article.price/100 }}&nbsp;€</div>
                    {% endif %}
                </div>
                {% endfor %}
            </div>

            <!-- Section: options -->
            <h4 class="mt-4">Options supplémentaires</h4>
            <p class="fst-italic text-secondary">Ces options permettent d’augmenter la valeur de la prestation.</p>
            {% for upsell in devis.upsells %}
                <div class="li-row">
                    <div>• {{ upsell.name }} — {{ upsell.description }}</div>
                    {% if upsell.price is not null %}
                        <div class="price">{{ upsell.price/100 }}&nbsp;€</div>
                    {% endif %}
                </div>
            {% endfor %}

            <!-- Totaux -->
            <div class="row mt-4">
                <div class="col-md-6">
                    <div class="total-card">
                        <div class="d-flex justify-content-between">
                            <div class="text-secondary">Options supplémentaires</div>
                            <div class="fw-semibold">+{{ sumUpsells }}&nbsp;€</div>
                        </div>
                        <div class="d-flex justify-content-between">
                            <div class="text-secondary">TVA ({{  devis.tvaRate }}%)</div>
                            <div class="fw-semibold">{{ tvaUpsells }}&nbsp;€</div>
                        </div>
                        <hr class="divider">
                        <div class="d-flex justify-content-between fs-5">
                            <div class="fw-semibold">Total TTC hors options</div>
                            <div class="fw-bold">{{ sumArticles+tvaArticles }}&nbsp;€</div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 mt-3 mt-md-0">
                    <div class="total-card">
                        <div class="d-flex justify-content-between">
                            <div class="text-secondary">Upsells</div>
                            <div class="fw-semibold">+{{ sumUpsells }}&nbsp;€</div>
                        </div>
                        <div class="d-flex justify-content-between">
                            <div class="text-secondary">Nouveau total HT</div>
                            <div class="fw-semibold">{{ subtotal }}&nbsp;€</div>
                        </div>
                        <div class="d-flex justify-content-between">
                            <div class="text-secondary">TVA ({{  devis.tvaRate }}%)</div>
                            <div class="fw-semibold">{{ tvaTotal }}&nbsp;€</div>
                        </div>
                        <hr class="divider">
                        <div class="d-flex justify-content-between fs-5">
                            <div class="fw-semibold">Total TTC avec options</div>
                            <div class="fw-bold">{{  tvaTotal+subtotal }}&nbsp;€</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Avis + garanties -->
            <div class="row mt-4 g-4">
                <div class="col-md-6">
                    {% if user.isDisplayAvisGoogle is true %}
                    <div class="stars mb-1">
                        <i class="bi bi-star-fill"></i>
                        <i class="bi bi-star-fill"></i>
                        <i class="bi bi-star-fill"></i>
                        <i class="bi bi-star-fill"></i>
                        <i class="bi bi-star-fill"></i>
                        <span class="ms-2 fw-semibold">5,0/5</span> <span class="text-secondary">sur</span> <strong>Google</strong>
                    </div>
                    <a href="{{ user.avisGoogle }}" target="_blank" class="text-decoration-none">Consultez nos avis en ligne</a>
                    {% endif %}
                    <ul class="mt-3 text-secondary">
                        <li>Satisfaction garantie</li>
                        <li>Entreprise assurée & déclarée</li>
                        <li>Interventions conformes</li>
                    </ul>
                </div>
                <div class="col-md-6">
                    <div class="text-secondary small">
                        <p class="mb-2">Devis valable {{ devis.validityDevis }} jours. Eau et électricité doivent être disponibles pour réaliser la prestation.</p>
                        <p class="mb-0">
                            {{ devis.mentionsLegales }}
                        </p>
                    </div>
                </div>
            </div>

        </div>
</main>
{% endblock %}
