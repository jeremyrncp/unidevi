{% extends 'base_dashboard.html.twig' %}

{% block title %}Création de devis — Choix du mode{% endblock %}

{% block body %}
<main class="container py-4">
<!-- Title -->
    <div class="d-flex justify-content-between align-items-center mb-2">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0">
                <li class="breadcrumb-item"><a href="{{ path("app_devis") }}" class="text-decoration-none">Style</a></li>
                <li class="breadcrumb-item active" aria-current="page">Mode</li>
                <li class="breadcrumb-item">Saisie</li>
                <li class="breadcrumb-item">Aperçu</li>
            </ol>
        </nav>
        <div class="avatar-doc" title="Mes devis">
            <i class="bi bi-card-checklist fs-4"></i>
        </div>
    </div>

    <div class="text-center my-3">
        <h1 class="display-5 fw-bold">Comment veux-tu créer ton devis&nbsp;?</h1>
        <p class="lead lead-muted mb-0">Tu pourras modifier toutes les informations ensuite.</p>
    </div>

    <div class="row g-4 mt-2">
        <!-- Saisir manuellement -->
        <div class="col-12 col-md-6">
            <div class="option h-100" role="button" data-mode="manual">
                <div class="d-flex align-items-center gap-3 mb-3">
                    <i class="bi bi-pencil-fill fs-2 text-dark"></i>
                    <h3 class="h4 mb-0">Saisir manuellement</h3>
                </div>
                <p class="text-secondary mb-4">Ajoute tes prestations une par une. Contrôle total.</p>
                <a class="btn btn-ghost px-4" type="button">Choisir ce mode</a>
            </div>
        </div>

        <!-- Générer avec l'IA -->
        <div class="col-12 col-md-6">
            <div class="option h-100 active" role="button" data-mode="ai">
                <div class="d-flex align-items-center gap-3 mb-3">
                    <i class="bi bi-lightning-charge-fill fs-2" style="color:var(--accent)"></i>
                    <h3 class="h4 mb-0">Générer avec l’IA</h3>
                </div>
                <p class="text-secondary mb-4">Décris la mission + temps estimé. L’IA s’occupe du reste.</p>
                <a class="btn btn-brand px-4 text-white" type="button">Choisir ce mode</a>
            </div>
        </div>
    </div>
</main>
{% endblock %}
