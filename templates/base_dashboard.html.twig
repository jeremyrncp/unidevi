<!DOCTYPE html>
<html lang="fr" data-bs-theme="light">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>{% block title %}Welcome!{% endblock %} - Unidevi</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        <!-- Bootstrap 5.3 -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <!-- Bootstrap Icons -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
        <link href="/css/styles.css" rel="stylesheet">

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        <div class="app-shell">
            <!-- Top bar -->
            <header class="app-header">
                <div class="container-fluid">
                    <div class="d-flex align-items-center justify-content-between">
                        <div class="d-flex align-items-center">
                            <span class="brand-dot"></span>
                            <strong><a href="/">Mon espace client</a></strong>
                        </div>
                        <nav class="d-flex align-items-center gap-4">
                            <a class="link-dark link-offset-2 link-underline-opacity-0 link-underline-opacity-75-hover" href="{{ path("app_profil") }}">Mon profil</a>
                            <a class="link-dark link-offset-2 link-underline-opacity-0 link-underline-opacity-75-hover" href="/logout">Déconnexion</a>
                            {% if user.avatar is not null %}<span class="avatar" style="background:#f9d1d1 url('/uploads/{{ user.avatar }}') center/cover;" aria-label="Photo profil"></span>{% endif %}
                        </nav>
                    </div>
                </div>
            </header>

            {% set flashMessages = app.session.flashbag.get('message') %}
            {% if flashMessages|length > 0 %}
                <div class="alert alert-success" role="alert">
                    <ul>
                        {% for flashMessage in flashMessages %}
                            <li>{{ flashMessage }}</li>
                        {% endfor %}
                    </ul>
                </div>
            {% endif %}

            {% set flashMessages = app.session.flashbag.get('error') %}
            {% if flashMessages|length > 0 %}
                <div class="alert alert-danger" role="alert">
                    <ul>
                        {% for flashMessage in flashMessages %}
                            <li>{{ flashMessage }}</li>
                        {% endfor %}
                    </ul>
                </div>
            {% endif %}

            {% block body %}{% endblock %}
            <!-- Footer -->
            <div class="footer">
                <span>Aide et support</span>
                <span>Version 1.0</span>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
